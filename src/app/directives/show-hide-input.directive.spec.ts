import { ShowHideInputDirective } from './show-hide-input.directive';

describe('ShowHideInputDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowHideInputDirective();
    expect(directive).toBeTruthy();
  });
});
